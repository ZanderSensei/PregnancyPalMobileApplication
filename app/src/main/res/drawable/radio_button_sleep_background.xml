<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/purple_200" /> <!-- Specific color for sleep hours -->
            <corners android:radius="4dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Default color for unchecked state -->
            <corners android:radius="4dp" />
        </shape>
    </item>
</selector>
